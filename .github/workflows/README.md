# ⚙️ Settings for workflow
`Actions` > `General` > `Workflow permissions`
- Read and write permissions

`Secrets and variables` > `Actions` > `New repository secret`
- DOCKERHUB_TOKEN
- DOCKERHUB_USERNAME
